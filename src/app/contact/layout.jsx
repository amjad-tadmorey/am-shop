export const metadata = {
    title: "Contact Us",
}

const layout = ({children}) => {
    return (
        <section id="contact">
            {children}
        </section>
    )
}

export default layout